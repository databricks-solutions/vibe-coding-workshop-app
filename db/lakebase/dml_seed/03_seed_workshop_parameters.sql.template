-- =============================================================================
-- SEED DATA: WORKSHOP PARAMETERS (TEMPLATE)
-- =============================================================================
-- Generated by: vibe2value configure
-- Source: 03_seed_workshop_parameters.sql.template
--
-- Placeholders are replaced with values from user-config.yaml
-- =============================================================================

-- Clear existing data (for recreate scenarios)
DELETE FROM ${schema}.workshop_parameters WHERE param_key IN ('workspace_url', 'workspace_org_id', 'default_warehouse', 'lakebase_instance_name', 'lakebase_host_name', 'lakehouse_default_catalog', 'model_serving_endpoint', 'chapter_3_lakehouse_catalog', 'chapter_3_lakehouse_schema', 'company_name', 'lakebase_uc_catalog_name', 'app_name');

-- Parameter 1: Databricks Workspace URL
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('workspace_url', 
 'Databricks Workspace URL', 
 '__WORKSPACE_URL__',
 'The URL of your Databricks workspace. Used for authentication commands and API calls. Format: https://your-workspace.cloud.databricks.com',
 'url',
 1,
 TRUE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 1b: Workspace Org ID (for ?o= query parameter in workspace URLs)
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('workspace_org_id', 
 'Workspace Org ID', 
 '__WORKSPACE_ORG_ID__',
 'The numeric workspace/org ID used in the ?o= query parameter for Databricks workspace URLs. You can find this in any workspace URL after ?o=.',
 'text',
 1,
 TRUE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 2: Default SQL Warehouse
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('default_warehouse', 
 'Default SQL Warehouse', 
 '__DEFAULT_WAREHOUSE__',
 'The name of the SQL Warehouse to use for queries and Lakebase operations. This should be an existing warehouse in your workspace.',
 'text',
 2,
 FALSE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 3: Lakebase Instance Name
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('lakebase_instance_name', 
 'Lakebase Instance Name', 
 '__LAKEBASE_INSTANCE_NAME__',
 'The name of the Lakebase PostgreSQL instance for your application. This is used for OLTP database operations and appears in Steps 5 and 6.',
 'text',
 3,
 TRUE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 4: Lakebase Host Name
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('lakebase_host_name', 
 'Lakebase Host Name', 
 '__LAKEBASE_HOST__',
 'The DNS hostname of the Lakebase PostgreSQL instance. This is the endpoint used to connect to the database.',
 'text',
 4,
 TRUE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 5: Lakehouse Default Catalog
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('lakehouse_default_catalog', 
 'Lakehouse Default Catalog', 
 '__LAKEBASE_CATALOG__',
 'The default Unity Catalog that will be used to create schemas, tables, and other lakehouse objects. This catalog should already exist in your workspace.',
 'catalog',
 5,
 TRUE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 6: Model Serving Endpoint
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('model_serving_endpoint', 
 'Model Serving Endpoint', 
 '__SERVING_ENDPOINT__',
 'The default model serving endpoint for calling LLMs. This endpoint should be configured in your workspace under Serving. Common options: databricks-meta-llama-3-1-70b-instruct, databricks-claude-sonnet-4-5.',
 'endpoint',
 6,
 TRUE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 7: Lakehouse Source Catalog
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('chapter_3_lakehouse_catalog', 
 'Lakehouse Source Catalog', 
 'samples',
 'The catalog containing sample data tables used for table metadata extraction in Step 10. This is the source for the Bronze layer schema definition.',
 'catalog',
 7,
 TRUE,
 TRUE,
 FALSE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 8: Lakehouse Source Schema
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('chapter_3_lakehouse_schema', 
 'Lakehouse Source Schema', 
 'wanderbricks',
 'The schema within the catalog containing sample data tables. This schema''s structure will be exported as the data dictionary for the Bronze layer.',
 'text',
 8,
 TRUE,
 TRUE,
 FALSE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 9: Company Name (Branding)
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('company_name', 
 'Company Name (Branding)', 
 '',
 'When set, UI design prompts (Figma and Cursor/Copilot) will include branding instructions to use this company''s colors and logo from brandcolorcode.com. Leave blank to skip branding.',
 'text',
 9,
 FALSE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 10: Lakebase UC Catalog Name
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('lakebase_uc_catalog_name', 
 'Lakebase UC Catalog Name', 
 '__LAKEBASE_UC_CATALOG__',
 'The Unity Catalog catalog name used to register the Lakebase PostgreSQL database for read-only access. Tables in this catalog are accessible via SQL, notebooks, and ETL pipelines.',
 'catalog',
 10,
 TRUE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);

-- Parameter 11: Databricks App Name
INSERT INTO ${schema}.workshop_parameters 
(param_key, param_label, param_value, param_description, param_type, display_order, is_required, is_active, allow_session_override, inserted_at, updated_at, created_by)
VALUES
('app_name', 
 'Databricks App Name', 
 '__APP_NAME__',
 'The name of the Databricks App deployed in the workspace. Used to construct verification links so users can navigate directly to their running app.',
 'text',
 11,
 FALSE,
 TRUE,
 TRUE,
 CURRENT_TIMESTAMP,
 CURRENT_TIMESTAMP,
 CURRENT_USER);
